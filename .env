# Contact number shown on the site. These are public placeholders (555-01xx
# numbers are reserved for fiction). The real number lives in .env.local,
# which is gitignored and overrides these when you run or build locally.
VITE_PHONE_AREA="(555)"
VITE_PHONE_LINE="555-0100"
VITE_PHONE_TEL="+15555550100"
